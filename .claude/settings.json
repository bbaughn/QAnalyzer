{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // \"\"' 2>/dev/null); case \"$f\" in *test_tracks/expectations.yaml) PYTHONPATH=. .venv/bin/python3.12 scripts/generate_test_summary.py >/dev/null 2>&1 ;; esac"
          }
        ]
      }
    ]
  }
}
